<form method="POST" action="{{ path('comment_create', {'parent': parent, 'type': type}) }}">

    {{ form_widget(form.body) }}
    {{ form_rest(form) }}

    <input type="submit" value="{{ 'tempo.form.create'|trans }}" class="pull-right btn btn-primary btn-sm" />
</form>
