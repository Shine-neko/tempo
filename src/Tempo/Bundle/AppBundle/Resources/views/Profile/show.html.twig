{% extends "TempoAppBundle::layout.html.twig" %}
{% block bodyClass %}user show events{% endblock %}

{% block container %}

    {% block breadcrumb %}
        <ol id="breadcrumb" class="clearfix">
            <li class="first"> <a href="{{ path('homepage') }}">{{ 'tempo.menu.home'|trans }}</a> </li>
            <li> <a href="{{ path('user_profile_edit') }}">Profile</a> </li>
            <li class="last"> {{ profile.username }} </li>
        </ol>
    {% endblock breadcrumb %}

    <div class="container-panel">

        <div class="col-md-2">
           <img src="{{ profile.avatar(220) }}" />
            <h1>{{ profile.firstName}}  {{ profile.lastName }} <br />
                <small>{{ profile.username }}</small>
            </h1>

            <hr />
        </div>
        <div class="col-md-8 app-panel">
            <ul class="nav nav-tabs" id="myTab">
                <li class="active"><a href="#activity" data-toggle="tab">Activity</a></li>
            </ul>

            <div class="tab-content">
                <div class="tab-pane active" id="activity">
                    {% for activity in activities %}
                        {% include 'TempoAppBundle:Activity:_user.html.twig' %}
                    {% endfor %}
                </div>
            </div>

        </div>
    </div>
{% endblock %}
