{% block content %}
    <form action="{{ path('timesheet_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit" class="fantomas btn btn-sm btn-primary">Edit</button>
        </p>
    </form>
{% endblock content %}
